[package]
name = "forkbox"
version = "0.5.0"
edition = "2021"

[dependencies]
nix = { version = "0.29", features = ["sched", "mount", "process", "resource", "fs", "signal", "user"] }
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"